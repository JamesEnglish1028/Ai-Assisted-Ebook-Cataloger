# Build script for Render deployment

# Install dependencies
npm install

# Build the frontend
npm run build

# The built files will be in dist/ directory
# The server will serve these static files along with the API